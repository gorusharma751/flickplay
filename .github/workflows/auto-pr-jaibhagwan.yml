name: Auto PR for Jaibhagwan Pushes

on:
  push:
    branches-ignore:
      - 'main'

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: auto-pr-${{ github.ref }}
  cancel-in-progress: false

env:
  ALLOWED_COLLABORATOR: jaibhagwanjindal,gorusharma751

  
jobs:
  create-or-reuse-pr:
    # Only run if the push is from your account
    if: github.actor == 'JaibhagwanJindal' || github.actor == 'jaibhagwanjindal'
    runs-on: ubuntu-latest
    steps:
      - name: Validate required secret
        env:
          OWNER_TOKEN: ${{ secrets.OWNER_TOKEN }}
        run: |
          if [ -z "$OWNER_TOKEN" ]; then
            echo "OWNER_TOKEN secret is missing in repository settings."
            exit 1
          fi

      - name: Create or Update Pull Request
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.OWNER_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const branch = context.ref.replace('refs/heads/', '');
            const head = `${owner}:${branch}`;
            const actor = context.actor;

            // 1. Check for existing open PR for this branch
            const openPrs = await github.rest.pulls.list({
              owner,
              repo,
              state: 'open',
              head,
              base: 'main'
            });

            if (openPrs.data.length > 0) {
              core.notice(`PR already exists for ${branch}: #${openPrs.data[0].number}`);
              return;
            }

            // 2. Create the PR if none exists
            const shortSha = context.sha.substring(0, 7);
            const title = `Contribution: ${branch} (${shortSha})`;
            const body = `Automatically generated PR for push by @${actor} to trigger Vercel Hobby build flow via Owner-signed merge.`;

            try {
              const pr = await github.rest.pulls.create({
                owner,
                repo,
                title,
                body,
                head: branch,
                base: 'main',
                maintainer_can_modify: true
              });

              core.notice(`Successfully created PR #${pr.data.number}`);
              
              // Optional: Add a label to help the merge script identify it
              await github.rest.issues.addLabels({
                owner,
                repo,
                issue_number: pr.data.number,
                labels: ['auto-merge']
              });
              
            } catch (error) {
              core.setFailed(`Failed to create PR: ${error.message}`);
            }